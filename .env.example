# Frontend env. Copy to .env.local.
# Leave BLANK for local dev — Vite proxies /api to the server on port 5000.
# In production set this to the deployed contact API, e.g.
#   VITE_API_BASE_URL=https://api.roadlinkconsultancy.com
VITE_API_BASE_URL=
